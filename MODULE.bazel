# TODO(intern-medium): Regenerate Bazel support for the v2 root-package layout.
# The v1 Bazel files (WORKSPACE, BUILD.bazel, wasmedge/BUILD.bazel) targeted the
# old `wasmedge/` sub-package and were removed in the v2 redesign (see PLAN.md
# Phase 9). Steps: bump rules_go/gazelle, point go_deps at the new go.mod, run
# `gazelle` to emit BUILD files for the root package, bump the wasmedge_c
# http_archive to a 0.17.x release, and restore the cgo copts/linkopts for
# libwasmedge (see the v1 files in git history for reference:
# `git show e5e7653 -- BUILD.bazel wasmedge/BUILD.bazel MODULE.bazel`).
module(
    name = "wasmedge_go",
    version = "2.0.0",
)
